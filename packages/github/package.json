{
  "name": "@pagewright/github",
  "version": "0.1.0",
  "type": "module",
  "description": "GitHub provider abstraction (OAuth App + GitHub App) — repo creation, atomic commits, Pages, and Actions, behind one swappable interface.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node tests/run.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
